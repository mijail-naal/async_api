version: '3.9'

services:
  fastapi:
    image: fastapi
    build: ../../fastapi
    container_name: "fastapi"
    command: ["/app/src/docker/app.sh"]
    ports:
      - "3000:3000"
    depends_on:
      - elasticsearch
      - redis
    env_file:
      - .env